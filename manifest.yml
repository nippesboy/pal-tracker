---
applications:
  - name: pal-tracker
    path: build/libs/pal-tracker.jar
    routes:
      - route: ((appName))-((reviewRoute)).((domain))
    env:
      WELCOME_MESSAGE: ${{ secrets.CF_APP_NAME }}
      JBP_CONFIG_OPEN_JDK_JRE: ((jreVersion))